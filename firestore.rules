rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo-only rules: allow the web app to queue emails.
    // Lock this down before production use.
    match /mail/{docId} {
      allow read: if true;
      allow create: if true;
      allow update, delete: if false;
    }
  }
}

