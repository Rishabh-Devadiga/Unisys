// Fill this from Firebase Console → Project settings → Your apps → Firebase SDK snippet (Config).
// Keep keys here only for local demo; don’t commit real production config.
window.FIREBASE_CONFIG = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
  // measurementId is optional (analytics)
  measurementId: "REPLACE_ME"
};
